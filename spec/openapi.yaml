openapi: 3.1.0
info:
  title: Transio API
  version: 1.0.0
  description: Bus transport platform API
servers:
  - url: http://localhost:3000
    description: Local development
paths: {}
components:
  schemas: {}
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
