# Progress Log

## Compacted History (Iterations 1–9)

### Completed Tasks
- **TASK-001**: Driver hooks (`use-driver-trips.ts`) — `useDriverTrips`, `useDriverTripDetail` with date-based filtering, 8 tests
- **TASK-002**: Driver trips list page — date nav, status badges (upcoming/in-progress/completed/cancelled), mobile-first, 18 tests
- **TASK-003**: Driver trip detail page — live map, geolocation toggle, GPS posting (5s interval via refs), stop progress tracker, passenger summary, 34 tests. Documented 3 spec gaps (missing busId, stop lat/lng, passenger list)
- **TASK-004**: Delay reporting page — preset minutes, custom input, reason dropdown, Zod validation, 25 tests
- **TASK-005**: Admin hooks (`use-admin.ts`) — `useAdminBuses`, `useToggleSeat` (invalidates both admin + provider keys), 9 tests
- **TASK-006**: Admin fleet page — buses grouped by providerId, expandable seat toggle grid, pagination, 28 tests
- **TASK-007**: Error boundary components — `ErrorBoundary` (class component wrapping Outlet in AppLayout), `ErrorFallback`, `NotFoundPage` (404 catch-all), 17 tests
- **TASK-008**: Shared loading/empty/error components — `PageError`, `EmptyState`, `CardGridSkeleton`, `CardListSkeleton`. Refactored 10 pages, removed ~60 inline duplicates (net -363 lines), 22 tests
- **TASK-009**: WCAG 2.1 AA audit — skip-to-content link, aria-live on tracking sidebar + delays list, vitest-axe integration, 8 new tests

### Key Patterns & Gotchas
- Run vitest from `apps/web/`, not monorepo root
- Follow existing page patterns: sub-components for skeleton/error/empty/card, section with sr-only h2
- No Badge UI component — use styled spans
- Avoid setState in effects (use lazy initializer); don't update refs during render (use effects)
- Use `format(new Date(...), 'HH:mm')` in tests for timezone independence
- `offsetMinutes !== ''` not `offsetMinutes ?` — `'0'` is falsy in JS
- Zod: use `required_error` on `z.enum` for custom messages; gives "Required" for undefined numbers, not min error
- `gridcell` role doesn't support `aria-pressed` — use descriptive aria-label
- Extract sub-components to stay under ESLint max complexity (15)
- Wrap useMemo deps on stable references, not derived `??` expressions
- ErrorBoundary in AppLayout (wrapping Outlet) > per-route boundaries
- Use `_error` prefix for unused params to satisfy lint
- When removing inline components, check all imports — other components may share them
- vitest-axe needs explicit `expect.extend(matchers)` in setup; use `import * as axeMatchers from 'vitest-axe/matchers'`
- Admin hooks follow same pattern as provider hooks; invalidate both admin + provider query keys for shared resources

---
## Iteration 10 - TASK-010: Optimize bundle and rendering
- All 22 routes already lazy-loaded via React.lazy in router.tsx
- Wrapped SeatMapEditor and EditorSeatCell in React.memo for seat grid performance
- Wrapped LiveMap and BusMarker in React.memo; memoized stopIcon with useMemo to avoid recreating DivIcon on each render
- Polling verified: useGpsPosting uses refs for non-visual state (positionRef, stopIndexRef) to avoid re-renders from interval ticks; React Query's refetchInterval only re-renders subscribing components
- Bundle size: 303.5KB gzipped total (258KB excluding leaflet) — well under 500KB target
- All 1084 tests pass, typecheck clean, lint clean (only pre-existing react-hooks/incompatible-library warnings)
- Learnings: Vite code-splitting via React.lazy already works well out of the box. The main optimization wins come from React.memo on components rendered in loops (EditorSeatCell in seat grid, BusMarker).
---
## Iteration 11 - TASK-011: Optimize API calls
- Audited all 22 React Query hooks across 11 hook files: staleTime properly tiered (lists: 60s, details: 30s, search: 30s, tracking: 5s, templates: 5m)
- Updated 5 detail queries from 60s to 30s staleTime (bookings, buses, routes, schedules, driver-trips)
- Removed unused `useTracking` hook from use-provider-tracking.ts (duplicate of `useBusTracking` in use-tracking.ts)
- Confirmed zero raw fetch() calls, zero duplicate queries on same page, tracking polling stops on unmount via `enabled` flag
- 1081 tests pass, typecheck clean, lint clean
- Learnings: When two hooks share the same query key and endpoint (trackingKeys.detail), they create potential cache conflicts. Remove the unused one rather than maintaining duplicates with different polling intervals.
---
## Iteration 12 - TASK-012: XSS prevention audit
- Full audit confirmed: zero dangerouslySetInnerHTML, zero raw .bindPopup(), zero dynamic href/src bindings, zero user input in style attrs
- Created `src/lib/sanitize-url.ts` with sanitizeUrl() helper: rejects javascript:, data:, vbscript: schemes; allows https:, http:, mailto:, tel:, and relative URLs
- 17 unit tests covering safe URLs (https, http, relative, mailto, tel, hash, query params), dangerous URLs (javascript, data, vbscript, case-insensitive, leading spaces), and edge cases (empty, whitespace, trimming, fragments)
- Leaflet map uses React Leaflet's `<Popup>` component (auto-escapes). Toast uses Radix UI primitives (plain text only)
- No user-provided URLs currently rendered in href/src — sanitizeUrl is ready for future use
- 1098 tests pass, typecheck clean, lint clean
- Learnings: Current codebase has no XSS vectors — React auto-escapes JSX, React Router `to` prop doesn't support javascript: scheme, Leaflet uses React components. The sanitizeUrl helper is preventive for when user-provided URLs (avatarUrl, logos) are added.
---

## Iteration 13 - TASK-013: Subresource Integrity for CDN assets
- Leaflet CSS already bundled locally via `import 'leaflet/dist/leaflet.css'` in live-map.tsx — no CDN dependency
- index.html has zero external CDN `<link>` or `<script>` tags
- Only external URL is CartoDB tile server (`basemaps.cartocdn.com`) which serves dynamic map tiles at runtime — not a static asset eligible for SRI
- No code changes needed — all criteria already satisfied
- Learnings: Check existing imports before assuming CDN usage. Vite bundles CSS imports from node_modules into the build output.
---

## Iteration 14 - TASK-014: Secure token and sensitive data handling
- Access token already in memory only (module-level variable in client.ts) — no localStorage/sessionStorage
- Added `setOnForbiddenOrLocked` callback to API client middleware for 403/423 responses, wired in AuthProvider to call `clearAuth()`
- Added `useEffect` cleanup on all 4 auth pages (login, register, change-password, reset-password) to `reset()` form on unmount — clears password fields from memory
- All password creation fields already had `autocomplete="new-password"`, login has `autocomplete="current-password"`
- Zero console.log in codebase, no sensitive data in URL params (only one-time reset token)
- Added 5 new tests to client.test.ts for 403/423 callback handling
- Updated 2 test mocks (auth-context.test.tsx, root-layout.test.tsx) to include `setOnForbiddenOrLocked`
- 1102 tests pass, typecheck clean, lint clean
- Learnings: When adding new exports to a module, check all test files that mock that module — they'll break if the mock doesn't include the new export.
---

## Iteration 15 - TASK-015: OpenAPI spec conformance validation
- Created `scripts/api-check.js` — standalone Node ESM script that reads bundled OpenAPI spec, scans all source files for client.METHOD() calls, validates endpoints against spec, checks types freshness, reports coverage
- Script reports: 46 API calls across 94 source files, 41/47 spec endpoints covered (87.2%), 6 uncovered admin/provider endpoints
- Exit codes: 0 (pass), 1 (spec violations), 2 (stale types warning)
- 6 tests in `scripts/api-check.test.ts` including stale types detection (modifies types file, restores in afterAll)
- Added `scripts/**/*.ts` to tsconfig.node.json and vitest include for Node-typed test files outside src/
- Learnings: Test files using Node APIs (child_process, fs) must be under tsconfig.node.json (which has `types: ["node"]`), not tsconfig.app.json (browser types only). Place them in scripts/ directory.
---

## Learnings
- Run vitest from `apps/web/`, not monorepo root
- Follow existing page patterns (sub-components for skeleton/error/empty/card)
- No Badge UI component — use styled spans
- `'0'` is falsy: use `!== ''` for string-to-number guards
- Zod: `required_error` on `z.enum`; undefined numbers give "Required" not min error
- `gridcell` role doesn't support `aria-pressed`
- Extract sub-components for ESLint max complexity (15)
- ErrorBoundary wraps Outlet in AppLayout for clean isolation
- vitest-axe: explicit `expect.extend(matchers)` in setup
- Invalidate both admin + provider query keys for shared resources
- React.memo wins on loop-rendered components (EditorSeatCell, BusMarker)
- Remove duplicate hooks sharing same query key to avoid cache conflicts
- React auto-escapes JSX; sanitizeUrl is preventive for future user-provided URLs
- Vite code-splitting via React.lazy works well out of the box
- staleTime tiers: lists 60s, details 30s, search 30s, tracking 5s, templates 5m
- Bundle target: 303.5KB gzipped (258KB excl. leaflet), well under 500KB
- When adding new exports to a module, update all test mocks for that module
