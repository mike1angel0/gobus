# Progress Log

## Compacted History (Iterations 1–9)

### Setup & Tooling (Iterations 1–5)
- **TASK-001**: Scaffolded Vite + React 19 + TypeScript 5.9 strict. Path aliases `@/` → `src/`. Core deps: react-router-dom, @tanstack/react-query, react-hook-form, zod, @hookform/resolvers, date-fns.
- **TASK-002**: Tailwind CSS v3.4 + shadcn/ui dark theme. Ported legacy utility classes (.glass, .glass-card, .btn-*, .input-field). shadcn components: button, input, card, dialog, toast, toaster. `cn()` in `src/lib/utils.ts`.
- **TASK-003**: ESLint flat config (complexity: 15, max-depth: 4, max-lines: 500, max-lines-per-function: 250, no-explicit-any: error, jsx-a11y recommended). Prettier (semi, singleQuote, printWidth: 100). shadcn components excluded from JSDoc rules.
- **TASK-004**: Vitest + RTL + jsdom. 90% coverage thresholds. `renderWithProviders()` helper wraps QueryClientProvider + MemoryRouter. shadcn files excluded from coverage.
- **TASK-005**: openapi-typescript v7 generates types from `spec/dist/openapi.json` → `src/api/generated/types.ts`. Script: `npm run api:sync`. Must run `npm run spec:bundle` first.

### API & State Layer (Iterations 6–8)
- **TASK-006**: openapi-fetch typed client (`src/api/client.ts`), ApiError class with RFC 9457 parsing (`src/api/errors.ts`), `useApiClient` hook. Access token in memory only (never localStorage). Auth middleware auto-attaches Bearer, fires onUnauthorized on 401. Must `response.clone()` before reading body in middleware.
- **TASK-007**: QueryClient defaults (staleTime: 30s, retry: 1, gcTime: 5min). 11 key factories in `src/api/keys.ts` (auth, route, bus, schedule, booking, tracking, search, driver, delay, admin, driverTrip) with `.all/.lists/.detail` pattern.
- **TASK-008**: AuthProvider with login/register/logout/changePassword/forgotPassword/resetPassword. Refresh token in localStorage, access token in memory. Auto-refresh 60s before JWT expiry. 401 during refresh → full logout; 403/423 during restore → silent clear.

### Auth Pages (Iteration 9)
- **TASK-009**: Login page with React Hook Form + Zod + zodResolver. API error handling for INVALID_CREDENTIALS/ACCOUNT_SUSPENDED/ACCOUNT_LOCKED. Field-level RFC 9457 error mapping. Role-based redirect. Used h1 for heading semantics (CardTitle is div). Shadcn Label manually installed.

### Key Patterns & Gotchas
- **react-refresh/only-export-components**: Never mix component exports with non-component exports (constants, schemas, contexts). Split into separate `.ts` files.
- **shadcn CLI**: Fails with "Invalid configuration" on this project — manually create components instead.
- **ESLint test overrides**: `max-lines-per-function: off` and `max-lines: off` for test files.
- **Complex forms**: Extract field groups into sub-components to stay under complexity/max-lines limits.
- **ApiError constructor**: Takes an `ErrorResponse` object, not individual args.
- **jsdom localStorage**: Must mock with `Object.defineProperty(globalThis, 'localStorage', {...})`.
- **CardTitle**: Renders as `<div>`, not a heading — use `<h1>` directly for accessibility.
- **openapi-fetch middleware**: Must `response.clone().json()` since body stream is single-consume.
- **tailwindcss-animate**: Regular dep, not devDep (imported at build time).
- **Coverage**: Exclude shadcn `use-toast.ts` (~180 lines generated code) alongside `components/ui/`.

---

## Iteration 10 - TASK-010: Create register page
- Created register-schema.ts with Zod validation matching OpenAPI spec (email max 255, name max 100, password min 8/max 128 with uppercase+lowercase+digit pattern, phone max 20, providerName max 200 conditional on PROVIDER role, confirmPassword match)
- Created register-fields.tsx with extracted RoleToggle, IdentityFields, PasswordFields components to keep main page under complexity limits
- Created register.tsx with role toggle (PASSENGER/PROVIDER), conditional provider fields, password strength indicator (weak/fair/strong with progressbar), API error mapping (EMAIL_ALREADY_EXISTS → email field, field-level RFC 9457 errors), auto-login after success
- 48 tests covering rendering, accessibility, password strength indicator, client-side validation (empty form, invalid email, password rules, confirm mismatch, provider without name), form submission (passenger, provider, phone), API errors (409, 400 field errors, generic), redirect
- Added max-lines: 'off' for test files in ESLint config (test files naturally exceed 500 lines)
- Learnings: For complex forms, extract field groups into separate components to stay under ESLint complexity/max-lines-per-function limits. React Compiler's react-hooks/incompatible-library warning on watch() is expected — React Hook Form's watch API can't be memoized but it's the standard way to observe field values. Use it.each for repetitive schema validation tests to save lines.
---

## Iteration 11 - TASK-011: Create forgot password and reset password pages
- Created forgot-password-schema.ts (email validation matching OpenAPI spec) and forgot-password.tsx (email form, success view with anti-enumeration message, network error handling)
- Created reset-password-schema.ts (password min 8/max 128 with uppercase+lowercase+digit, confirmPassword match) and reset-password.tsx (reads ?token= from URL, password strength indicator reusing getPasswordStrength from register, API error handling for TOKEN_EXPIRED/INVALID_TOKEN codes, field-level RFC 9457 errors, redirect to login on success)
- Extracted reset-password-error.tsx (TokenErrorView) to keep main component under ESLint complexity limit of 15
- 47 tests across both pages covering: rendering, accessibility (semantic headings, labels, aria-invalid, aria-describedby), client-side validation, form submission, success states, API errors (expired/invalid token, field errors, generic errors, network errors), password strength indicator, schema validation
- Learnings: ApiError constructor takes an ErrorResponse object, not individual args. Extract early-return views into separate components to reduce cyclomatic complexity below ESLint threshold.
---

## Iteration 12 - TASK-012: Create change password page
- Created change-password-schema.ts with Zod validation matching OpenAPI spec (currentPassword min 1/max 128, newPassword min 8/max 128 with uppercase+lowercase+digit pattern, confirmPassword match)
- Created change-password.tsx with current password field, new password + confirm with strength indicator (reusing getPasswordStrength from register-schema), form reset on success, success toast via shadcn useToast, API error mapping (401/INVALID_CREDENTIALS → currentPassword field error, field-level RFC 9457 errors, generic fallback)
- 26 tests covering: rendering, accessibility (labels, autocomplete, aria-invalid, aria-describedby), client-side validation (empty, short, weak pattern, mismatch), password strength indicator, form submission (success + toast + reset), API errors (401 with/without code, 400 field errors, 500 generic, non-API), schema validation
- Learnings: The `toast` function from shadcn can be imported standalone from `@/hooks/use-toast` without needing `useToast` hook — simpler for one-off success notifications. For change password, map 401 status directly to currentPassword field error regardless of error code, as the spec uses 401 for incorrect current password.
---

## Iteration 13 - TASK-013: Create React Router setup with auth guards
- Created AuthGuard (redirects unauthenticated to /auth/login with `from` state, renders nothing during loading to prevent flash) and RoleGuard (checks user role against allowedRoles, redirects to / on mismatch)
- Created router.tsx with createBrowserRouter, all routes lazy-loaded via React.lazy + Suspense. RootLayout wraps providers (QueryClient, Auth) inside the router tree so they have access to router context
- Moved providers from main.tsx into RootLayout component inside the router — necessary because createBrowserRouter renders outside the React tree from main.tsx
- Created placeholder.tsx for unimplemented routes
- 16 tests across auth-guard and role-guard covering: authenticated access, unauthenticated redirect, loading state (no flash), location state preservation, role matching/mismatch for all 4 roles
- Learnings: With createBrowserRouter, providers must be inside the router tree (RootLayout), not wrapping RouterProvider in main.tsx. Router config file (router.tsx) needs react-refresh/only-export-components disabled since it exports a non-component. React Compiler's react-hooks/immutability rule prevents mutating external variables in components — render state as text in test assertions instead of capturing to external refs.
---

## Iteration 14 - TASK-014: Create Navbar component
- Created navbar-links.ts with NavLink type, AUTH_NAV_LINKS (role-filtered), PUBLIC_NAV_LINKS, and getLinksForRole() utility
- Created navbar-mobile.tsx with slide-in mobile menu: backdrop, focus management (auto-focus close button on open), Escape key handler, role-based links, user name display, sign out / login+register for unauth
- Created navbar.tsx with responsive desktop/mobile layout: logo link, role-based nav links, active link highlighting via useLocation, user name + sign out (authenticated), login/register (unauthenticated), hamburger toggle with aria-expanded
- 29 tests covering: unauthenticated state (logo, public links, login/register), all 4 roles (PASSENGER, PROVIDER, DRIVER, ADMIN link visibility), active link highlighting, accessibility (landmarks, aria-labels, aria-expanded), mobile menu (open/close via button/backdrop/escape, role links, sign out, login/register, close on link click)
- Learnings: When checking CSS class absence, use `className.split(' ')` array check since `toContain('bg-accent')` matches `hover:bg-accent` too. Split mobile menu into separate component to keep navbar under ESLint complexity limits.
---

## Iteration 15 - TASK-015: Create app layout and home page
- Created app-layout.tsx wrapping Navbar + Outlet (in `<main>`) + Toaster in a flex-col min-h-screen container
- Created home.tsx with hero section (h1 heading, description), search form (origin, destination, date inputs with maxLength matching OpenAPI spec), and 3 feature cards (real-time tracking, secure booking, wide coverage) using glass-morphism design
- Updated router.tsx to nest all routes inside AppLayout, replaced PlaceholderPage at `/` with HomePage
- Removed unused eslint-disable directive from router.tsx (react-refresh/only-export-components no longer needed)
- 16 tests: app-layout (3 tests: renders navbar/outlet/toaster, main element wrapping, flex layout) + home (13 tests: rendering, accessibility, search form submission with params, responsive grid classes)
- Learnings: When running vitest from the monorepo root, path aliases don't resolve — must run from apps/web. The git working directory may shift during a session; use absolute paths for git commands.
---

## Iteration 16 - TASK-016: Create CLAUDE.md for frontend
- Expanded existing apps/web/CLAUDE.md from a minimal stub into comprehensive documentation
- Documented: tech stack table, quick start (monorepo-aware commands), full project structure, API-first workflow pipeline, React Query hook patterns, form patterns with Zod + RFC 9457 error mapping, component patterns, error handling, naming conventions, code quality rules, ESLint complexity gates, accessibility requirements, testing conventions, quality gate commands, known gotchas
- All 4 acceptance criteria met: sections documented, API-first workflow explained, pattern examples included, quality commands listed
- Learnings: CLAUDE.md already existed from TASK-001 scaffold — always check for existing file before creating new one.
---

## Iteration 17 - TASK-017: Run Phase 1 quality gates
- All quality gates pass: typecheck (0 errors), lint (0 errors, 3 expected react-hooks/incompatible-library warnings), format (all clean), tests (340 pass), coverage (95.51% stmts / 90.24% branches / 97.08% functions / 96.17% lines), build succeeds
- Fixed formatting across 16 files via prettier --write
- Excluded generated types (src/api/generated/**), App.tsx, and router.tsx from coverage (config/entry files like main.tsx)
- Added tests for: root-layout.tsx, placeholder.tsx, auth-types.ts (decodeJwtPayload, getTokenExpiryMs, getStoredRefreshToken, storeRefreshToken), auth-context.tsx (restore with no data, 403/423 during restore, login/register with no data)
- Zero `any` types in src/ (excluding test), all exports have JSDoc, all components have accessibility attributes
- Learnings: openapi-fetch captures fetch reference at module load time — cannot simply override globalThis.fetch in tests. Test middleware indirectly through mocked API client in integration tests instead.
---

## Learnings
See "Key Patterns & Gotchas" in Compacted History above, plus iteration-specific learnings in each iteration entry.
