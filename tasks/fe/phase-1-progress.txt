## Iteration 1 - TASK-001: Initialize Vite + React + TypeScript strict
- Scaffolded Vite project with React 19 + TypeScript 5.9.3 strict mode
- Configured path aliases (@/ → src/) in tsconfig.app.json and vite.config.ts
- Installed core deps: react-router-dom, @tanstack/react-query, react-hook-form, zod, @hookform/resolvers, date-fns
- Scripts: dev, build, preview, typecheck all working
- Learnings: Vite create template uses React 19 (not 18) — fine, backwards compatible. TypeScript 5.9 ships with strict by default in template.
---

## Iteration 2 - TASK-002: Configure Tailwind CSS + shadcn/ui
- Installed Tailwind CSS v3.4, PostCSS, Autoprefixer, tailwindcss-animate
- Replicated exact color palette from _legacy/tailwind.config.ts (primary, accent, dark)
- Ported all utility classes from _legacy/src/app/globals.css: .glass, .glass-card, .btn-primary, .btn-accent, .btn-secondary, .input-field
- Added shadcn/ui CSS variables for dark theme, configured components.json
- Installed shadcn components: button, input, card, dialog, toast, toaster
- Created src/lib/utils.ts with cn() helper
- Learnings: shadcn v4 CLI creates `@/` as a literal directory when path aliases aren't properly resolved. Had to move files manually. Added resolvedPaths to components.json to fix future installs. Also `tailwindcss-animate` should be in regular deps (not dev) since it's imported in tailwind.config.js which is processed at build time.
---

## Iteration 3 - TASK-003: Configure ESLint, Prettier, and quality gates
- Installed eslint-plugin-jsx-a11y, eslint-plugin-jsdoc, prettier
- Configured ESLint flat config with: complexity gates (max-lines: 500, max-lines-per-function: 250, complexity: 15, max-depth: 4), JSDoc on exports, no-explicit-any as error, jsx-a11y recommended
- Excluded shadcn/ui components and config files from JSDoc requirements (generated code)
- Configured Prettier: semi, singleQuote, printWidth: 100
- Added scripts: lint, format:check, format:fix
- Auto-formatted existing files with Prettier
- All quality gates pass: lint, format:check, typecheck
- Learnings: shadcn/ui generated components trigger JSDoc/react-refresh/unused-vars warnings — exclude them via file-specific config overrides rather than disabling rules globally.
---

## Iteration 4 - TASK-004: Set up Vitest + React Testing Library
- Installed vitest, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom, @vitest/coverage-v8
- Created vitest.config.ts with jsdom environment, @/ path alias, v8 coverage with 90% thresholds
- Created src/test/setup.ts importing jest-dom/vitest matchers
- Created src/test/helpers.tsx with renderWithProviders() wrapping QueryClientProvider + MemoryRouter
- Added test, test:watch, test:coverage scripts
- Excluded shadcn-generated files (use-toast.ts, utils.ts, components/ui/) from coverage to avoid false negatives
- Added vitest/globals types to tsconfig.app.json for global describe/it/expect
- Smoke test for App component passes, coverage at 100% for non-excluded source
- Learnings: shadcn's use-toast.ts is ~180 lines of generated code that tanks coverage if included. Exclude it alongside components/ui/ in vitest coverage config.
---

## Iteration 5 - TASK-005: Set up OpenAPI type generation
- Installed openapi-typescript v7.13 as devDep
- Created `npm run api:sync` script that generates types from `../../spec/dist/openapi.json` to `src/api/generated/types.ts`
- Created `src/api/generated/.gitkeep` for directory persistence
- Generated 4241 lines of types covering all 30+ API paths and 47 operations
- Created `apps/web/CLAUDE.md` documenting API-first workflow and type generation pipeline
- Added `coverage` to ESLint globalIgnores (was causing spurious warnings)
- Learnings: openapi-typescript v7 generates clean types including paths, operations, and components interfaces. The bundled spec at `spec/dist/openapi.json` must exist before running api:sync — run `npm run spec:bundle` from root first.
---

## Iteration 6 - TASK-006: Create typed API client
- Installed openapi-fetch, created src/api/client.ts (typed client with auth middleware), src/api/errors.ts (ApiError class, RFC 9457 parsing), src/api/hooks.ts (useApiClient hook)
- Access token stored in memory only (setAccessToken/getAccessToken), never localStorage
- Auth middleware auto-attaches Bearer token and fires onUnauthorized callback on 401
- Error middleware parses RFC 9457 responses into ApiError instances, falls back to generic error for non-conforming responses
- 28 unit tests covering error parsing, type guards, token management, and hook stability
- Learnings: openapi-fetch middleware uses onRequest/onResponse pattern. The middleware must clone the response before reading the body (response.clone().json()) since the body stream can only be consumed once. Keep token in module-level variable for simplicity — auth context will call setAccessToken().
---

## Iteration 7 - TASK-007: Create React Query configuration and key factories
- Created src/providers/query-client.ts with QueryClient defaults (staleTime: 30s, retry: 1, refetchOnWindowFocus: false, gcTime: 5min)
- Created src/providers/query-provider.tsx with AppQueryProvider component
- Created src/api/keys.ts with 11 key factories: authKeys, routeKeys, busKeys, scheduleKeys, bookingKeys, trackingKeys, searchKeys, driverKeys, delayKeys, adminKeys, driverTripKeys
- Each factory has .all, .lists(filters?), .detail(id) for hierarchical cache invalidation
- Wired AppQueryProvider into main.tsx wrapping the App
- 90 tests passing (62 new for keys + provider)
- Learnings: react-refresh/only-export-components ESLint rule forbids mixing component exports with non-component exports (like QueryClient). Split constants into a separate .ts file and keep only the component in the .tsx file.
---
