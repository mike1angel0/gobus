## Iteration 1 - TASK-001: Create provider domain hooks
- Created 6 hook files: use-routes.ts, use-buses.ts, use-drivers.ts, use-schedules.ts, use-provider-tracking.ts, use-delays.ts
- All hooks follow existing patterns from use-bookings.ts: typed API client, query key factories, toast notifications, isApiError error handling
- 57 tests across 6 test files, all passing
- Typecheck and lint clean
- Documented spec gap: no provider-level batch tracking endpoint (GET /api/v1/tracking). Worked around with parallel per-bus requests in useProviderTracking()
- Learnings: The useProviderTracking hook aggregates individual bus tracking calls — when a batch endpoint is added to the spec, this can be simplified
---

## Iteration 2 - TASK-002: Create provider dashboard page
- Created dashboard page at src/pages/provider/dashboard.tsx with stat cards, upcoming schedules list, and quick actions
- Updated router.tsx to lazy-load the dashboard at /provider index route
- Stat cards show totals for routes, buses, drivers, and active schedules by fetching list endpoints with pageSize=1
- Revenue chart and occupancy stats deferred — documented spec gap for missing analytics endpoint (GET /api/v1/provider/analytics)
- Refactored to reduce cyclomatic complexity: extracted useDashboardData hook, StatsSection, UpcomingSchedulesSection, QuickActionsSection
- 15 tests covering loading, loaded, empty, error, retry, accessibility, responsive layout, and data fetching assertions
- Learnings: ESLint complexity rule is strict (max 15) — proactively extract sub-components and custom hooks for pages with multiple queries
---

## Iteration 3 - TASK-003: Create route management page
- Created src/pages/provider/routes.tsx with route list cards, create dialog (name + stops builder), delete with confirmation
- Stops builder supports add, remove, reorder (up/down arrows), with lat/lng coordinate inputs
- Form validation matches OpenAPI spec: name 1-200 chars, stops min 2 max 100, lat -90..90, lng -180..180, orderIndex 0-based
- Updated router.tsx to replace PlaceholderPage with ProviderRoutesPage at /provider/routes
- 23 tests covering loading, loaded, empty, error, retry, create dialog flow, form validation, delete confirmation, accessibility, responsive layout
- All icons aria-hidden, sections with aria-labelledby, delete buttons with descriptive aria-labels
- Learnings: Tests must be run from apps/web/ directory (cd apps/web && npx vitest run), not monorepo root, for @ path aliases to resolve
---

## Iteration 4 - TASK-004: Create fleet management page
- Created src/pages/provider/fleet.tsx with bus list cards showing license plate, model, capacity, seat grid preview
- Extracted src/components/fleet/create-bus-dialog.tsx with template selector and manual config modes
- SeatGridPreview component shows mini seat grid with type-based colors (standard/premium/accessible/blocked)
- Create dialog supports: template selection with visual cards, manual row/column config with live preview
- Delete with confirmation warns about schedule impact
- Form validation matches OpenAPI spec: licensePlate 1-20, model 1-100, rows 1-100, columns 1-10
- Handles 409 license plate conflict error with field-level message
- Updated router.tsx to replace PlaceholderPage with ProviderFleetPage at /provider/fleet
- 29 tests covering all states, create flows (template + manual), validation, delete, accessibility
- Edit mode with seat map editor deferred to TASK-005 (separate acceptance criterion)
- Learnings: Extract dialog/form components into separate files proactively to stay under max-lines (500) and complexity (15) limits
---

## Iteration 5 - TASK-005: Create seat map editor component
- Created src/components/fleet/seat-map-editor.tsx with two interaction modes: click-to-cycle and brush toolbar for direct type selection
- Created src/components/fleet/edit-bus-dialog.tsx wrapping the editor in a dialog with bus detail loading
- Added edit button (pencil icon) to each bus card in fleet.tsx
- Brush toolbar uses radiogroup pattern for accessibility with Cycle/Standard/Premium/Accessible/Blocked options
- Visual distinction: type-specific colors, icons (star for premium, wheelchair for accessible, X for blocked)
- Seat count summary updates in real-time as user edits
- Save button disabled until changes are detected (deep comparison of seat types)
- 26 component tests covering rendering, click cycling, brush mode, keyboard navigation, save/cancel, accessibility
- Documented spec gap: PUT /api/v1/buses/{id} doesn't accept seats field — save shows toast notification until spec is updated
- Learnings: Tests must run from apps/web/ directory; the editor component doesn't need the booking SeatMap's complexity (no occupied/selected states) — kept it focused on type editing only
---

## Iteration 6 - TASK-006: Create schedule management page
- Created src/pages/provider/schedules.tsx with filter bar, schedule cards, driver assign dialog, cancel with confirmation
- Extracted src/components/schedules/create-schedule-dialog.tsx with route/bus/driver dropdowns, departure/arrival times, days of week toggle, base price, auto-calculated stop times from route
- Extracted src/components/schedules/schedule-filter-bar.tsx for route, bus, status dropdowns and date range inputs
- Filter bar passes filters to useSchedules hook with query params matching OpenAPI spec
- Schedule cards show route name, bus plate, times, price, days of week, driver status, status badge
- Create form validates: required fields, arrival > departure, price 0-100000, route with min 2 stops
- Stop times auto-calculated by evenly distributing travel time across route stops
- Driver assign dialog supports assign, change, and unassign (null) via useUpdateSchedule
- Cancel with confirmation dialog using useCancelSchedule
- Updated router.tsx to replace PlaceholderPage with ProviderSchedulesPage at /provider/schedules
- 27 tests covering loading, error, empty, loaded states, filter bar, cancel flow, driver assignment, create dialog, accessibility
- Learnings: ESLint complexity max 15 is strict — extract custom hooks for data fetching (useSchedulesPageData, useCreateScheduleData), separate validation/error-handling into standalone functions, and extract ScheduleListContent to eliminate conditional render branches from the page component
---

## Iteration 7 - TASK-007: Create driver management page
- Created src/pages/provider/drivers.tsx with driver list cards showing name, email, phone, assigned schedule count
- Extracted src/components/drivers/create-driver-dialog.tsx with name, email, password, phone fields
- Form validation matches OpenAPI spec: name 1-100, email max 255 with format check, password 8-128 with uppercase/lowercase/digit pattern, phone max 20
- Handles 409 email conflict error with field-level message on email input
- Delete with confirmation dialog warns about schedule unassignment impact when assignedScheduleCount > 0
- Updated router.tsx to replace PlaceholderPage with ProviderDriversPage at /provider/drivers
- 28 tests covering loading, error, empty, loaded states, create dialog with all validations, 409 conflict handling, delete confirmation with schedule warning, accessibility, responsive layout
- Learnings: Use noValidate on forms with type="email" inputs to prevent browser HTML5 validation from blocking our custom validation in tests; use ApiError class instances (not plain objects) when simulating API errors in tests since isApiError uses instanceof; wrap async state updates from error callbacks in waitFor
---

## Iteration 8 - TASK-008: Create provider tracking page
- Created src/pages/provider/tracking.tsx with full-width LiveMap, bus sidebar, delay reporting, and active delays list
- Extracted 3 components: tracking-bus-sidebar.tsx (bus list with real-time info), report-delay-dialog.tsx (delay form with Zod validation), active-delays-list.tsx (delays with deactivate button)
- Custom useTrackingPageData hook aggregates buses, tracking (5s polling via useProviderTracking), and active schedules
- Bus selection centers map on the selected bus; sidebar shows speed, current stop, last updated time
- Delay form validates against OpenAPI spec: offsetMinutes 1-1440, reason enum, note max 500 chars, required scheduleId/tripDate
- Active delays section appears when a bus with a schedule is selected, with deactivate button using useUpdateDelay
- Responsive layout: flex-col on mobile (map on top, sidebar below), flex-row on desktop (map fills left, sidebar 320px right)
- Updated router.tsx to replace PlaceholderPage with ProviderTrackingPage at /provider/tracking
- 30 tests covering loading, error, empty, loaded states, bus selection, delay reporting form with validation, active delays with deactivate, accessibility, responsive layout, data fetching
- Mocked LiveMap component in tests since leaflet requires DOM environment not available in jsdom
- Learnings: Wrap intermediate variables from React Query in useMemo to avoid ESLint react-hooks/exhaustive-deps warnings about logical expressions in dependency arrays; mock complex DOM-dependent components (like leaflet maps) with simple divs in tests
---
