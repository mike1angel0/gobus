# Phase 1: Frontend Foundation — Scaffold, API Client, Auth, Shell

**Status**: Complete
**Dependencies**: BE Phase 1 (OpenAPI spec designed, auth endpoints available)
**Goal**: Set up Vite + React frontend with TypeScript strict, generate typed API client from the shared OpenAPI spec, implement auth flow, app shell with role-based navigation, and quality gates.

---

## Scaffold & Tooling

### ~~TASK-001: Initialize Vite + React + TypeScript strict~~ ✅

### ~~TASK-002: Configure Tailwind CSS + shadcn/ui — replicate existing design~~ ✅

### ~~TASK-003: Configure ESLint, Prettier, and quality gates~~ ✅

### ~~TASK-004: Set up Vitest + React Testing Library~~ ✅

---

## API Client (Generated from OpenAPI Spec)

### ~~TASK-005: Set up OpenAPI type generation~~ ✅

### ~~TASK-006: Create typed API client~~ ✅

### ~~TASK-007: Create React Query configuration and key factories~~ ✅

---

## Auth Flow

### ~~TASK-008: Create auth context and hooks with secure token handling~~ ✅

### ~~TASK-009: Create login page~~ ✅

### ~~TASK-010: Create register page~~ ✅

### ~~TASK-011: Create forgot password and reset password pages~~ ✅

### ~~TASK-012: Create change password page~~ ✅

---

## App Shell & Navigation

### ~~TASK-013: Create React Router setup with auth guards~~ ✅

### ~~TASK-014: Create Navbar component~~ ✅

### ~~TASK-015: Create app layout and home page~~ ✅

---

## Quality & Documentation

### ~~TASK-016: Create CLAUDE.md for frontend~~ ✅

### ~~TASK-017: Run Phase 1 quality gates~~ ✅

---

## QA Batch 1 — Automated Quality Analysis

> Generated by QA agent. Coverage: 95.51% stmts | 90.24% branch | 97.08% funcs | 96.17% lines.
> Type safety: 0 `any` usages. Lint: 0 errors, 3 warnings (informational).

### Accessibility

#### US-QA-001: Fix a11y: home.tsx decorative icons missing aria-hidden

**ACs**
1. All decorative icons (MapPin, Calendar, Clock, Shield, Bus) in `pages/home.tsx` have `aria-hidden="true"`.
2. Existing tests pass without regressions.

#### US-QA-002: Fix a11y: home.tsx FeatureCard uses non-semantic container

**ACs**
1. `FeatureCard` in `pages/home.tsx` uses `<article>` instead of `<div>` as its root element.
2. Existing tests pass without regressions.

#### US-QA-003: Fix a11y: password strength aria-label missing strength level

**ACs**
1. Password strength progress bars in `change-password.tsx` and `reset-password.tsx` include the strength level in `aria-label` (e.g., `"Password strength: Strong"`), matching the pattern in `register-fields.tsx`.
2. Existing tests pass without regressions.

### Coverage

#### US-QA-004: Add tests for api/client.ts interceptors

**ACs**
1. Tests cover the response interceptor error handling and token refresh logic (lines 52–72) in `api/client.ts`.
2. Statement coverage for `api/client.ts` reaches ≥ 80%.

#### US-QA-005: Add tests for auth-context.tsx uncovered branches

**ACs**
1. Tests cover the uncovered paths in `auth-context.tsx` (lines 107–108 and 200–201).
2. Branch coverage for `auth-context.tsx` reaches ≥ 85%.

### JSDoc

#### US-QA-006: Add JSDoc to pages/auth/register-schema.ts

**ACs**
1. All exports (`PasswordStrength`, `getPasswordStrength`, `registerSchema`, `RegisterFormValues`) have JSDoc comments.
2. JSDoc includes `@param` and `@returns` where applicable.

#### US-QA-007: Add JSDoc to pages/auth/login-schema.ts

**ACs**
1. All exports (`loginSchema`, `LoginFormValues`, `getRedirectForRole`) have JSDoc comments.
2. `getRedirectForRole` includes `@param` and `@returns`.

#### US-QA-008: Add JSDoc to pages/auth/reset-password-schema.ts

**ACs**
1. All exports (`resetPasswordSchema`, `ResetPasswordFormValues`) have JSDoc comments.
2. Schema JSDoc describes validation rules.

#### US-QA-009: Add JSDoc to pages/auth/change-password-schema.ts

**ACs**
1. All exports (`changePasswordSchema`, `ChangePasswordFormValues`) have JSDoc comments.
2. Schema JSDoc describes validation rules.

#### US-QA-010: Add JSDoc to pages/auth/forgot-password-schema.ts

**ACs**
1. All exports (`forgotPasswordSchema`, `ForgotPasswordFormValues`) have JSDoc comments.
2. Schema JSDoc describes validation rules.

#### US-QA-011: Add JSDoc to pages/auth/register-fields.tsx

**ACs**
1. All exports (`RoleToggle`, `IdentityFields`, `PasswordFields`) have JSDoc comments.
2. JSDoc describes each component's purpose and props.

#### US-QA-012: Add JSDoc to pages/auth/reset-password-error.tsx

**ACs**
1. `TokenErrorView` export has a JSDoc comment.
2. JSDoc describes the component's purpose and props.

#### US-QA-013: Add JSDoc to pages/home.tsx FeatureCard

**ACs**
1. `FeatureCard` export has a JSDoc comment.
2. JSDoc describes the component's purpose and props.

#### US-QA-014: Add JSDoc to hooks/use-toast.ts

**ACs**
1. All exports (`useToast`, `toast`, `reducer`) have JSDoc comments.
2. JSDoc includes `@returns` for `useToast` and `toast`.

### Complexity

#### US-QA-015: Refactor auth-context.tsx AuthProvider (294 lines > 250 limit)

**ACs**
1. `AuthProvider` component in `auth-context.tsx` is ≤ 250 lines after extracting helpers (e.g., token refresh logic, session restoration) into separate functions or files.
2. All existing auth-context tests pass without modification.
