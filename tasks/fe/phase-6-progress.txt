## Iteration 1 - TASK-001: Add security headers to nginx.conf
- Added all 7 security headers (CSP, X-Frame-Options, X-Content-Type-Options, HSTS, Referrer-Policy, Permissions-Policy, X-XSS-Protection) to nginx.conf server block
- All headers use `always` directive
- Nginx config syntax validated via Docker (upstream resolution error is expected outside docker-compose)
- No TypeScript code changed, typecheck still passes
- Learnings: nginx `add_header` in server block applies to all locations unless overridden; the `always` flag ensures headers are sent even on error responses
---

## Iteration 2 - TASK-002: Add gzip compression to nginx.conf
- Added gzip on, gzip_vary on, gzip_min_length 1000, and gzip_types for text/CSS/JSON/JS/XML to nginx.conf server block
- Nginx config syntax validated via Docker (upstream resolution error is expected outside docker-compose, same as TASK-001)
- Typecheck passes, no TS code changes
- Learnings: gzip directives go in the server block before location blocks; gzip_vary is important for CDN/proxy caching
---

## Iteration 3 - TASK-003: Add cache-busting for index.html
- Added nested `location = /index.html` inside the SPA fallback location with `Cache-Control: no-cache, no-store, must-revalidate`, `Pragma: no-cache`, `Expires: 0`
- Hashed assets location updated to use `always` directive on Cache-Control
- Nginx config syntax validated via Docker (upstream resolution error expected outside docker-compose)
- Typecheck passes, no TS code changes
- Learnings: nginx supports nested location blocks; exact-match `location =` takes priority for specific paths within a parent location
---

## Iteration 4 - TASK-004: Harden web Dockerfile
- Added HEALTHCHECK directive using `wget -q --spider http://127.0.0.1:8080/` (wget available in alpine, curl is not)
- Configured non-root user: nginx runs as uid=101 (nginx user), verified with `docker exec whoami`
- Changed listen port from 80 to 8080 (non-root can't bind privileged ports), updated docker-compose.yml port mapping
- Fixed nginx pid file path to /tmp/nginx.pid (writable by non-root)
- Added deferred upstream resolution via `set $api_upstream` + `resolver 127.0.0.11` to prevent startup crash when api service is unavailable
- Health check verified as "healthy" via `docker inspect --format='{{.State.Health.Status}}'`
- Learnings: Use 127.0.0.1 not localhost in healthchecks (IPv6 localhost resolution fails when nginx only listens on IPv4); nginx pid path in alpine is `/run/nginx.pid` not `/var/run/nginx.pid`; use variable-based proxy_pass to defer upstream DNS resolution
---

## Iteration 5 - TASK-005: Add meta tags to index.html
- Added description meta tag, theme-color (#2563eb from CSS primary), canonical URL, and full Open Graph tags (title, description, type, url, image) to index.html
- Verified favicon.svg exists in public/
- HTML-only change, typecheck passes
- Learnings: Primary color is defined as HSL in index.css (`217 91% 60%`), converts to hex `#2563eb` for theme-color meta tag
---

## Iteration 6 - TASK-006: Add dynamic page titles
- Created `usePageTitle(title: string)` hook in `src/hooks/use-page-title.ts` that sets `document.title` to `"Title | Transio"` on mount and reverts to `"Transio"` on unmount
- Applied the hook to all 25 page components across home, search, my-trips, profile, trip detail, placeholder, 4 admin pages, 7 provider pages, 3 driver pages, and 5 auth pages
- Wrote unit test with 3 assertions: sets title on mount, reverts on unmount, updates on prop change
- All 1269 tests pass, typecheck clean, lint clean
- Learnings: Tests must run from `apps/web/` directory for path alias resolution; vitest needs to be run with correct cwd
---

## Iteration 7 - TASK-007: Set up react-i18next with per-domain translation files
- Installed react-i18next, i18next, i18next-browser-languagedetector
- Created `src/i18n/config.ts` with i18next initialization: fallbackLng 'ro', supportedLngs ['ro', 'en'], 9 namespaces, localStorage detection with key `i18n_lang`
- Created 18 translation JSON files (9 namespaces × 2 languages) under `src/i18n/locales/{ro,en}/`
- Wrapped app with `I18nextProvider` in `root-layout.tsx`, also added to test helpers
- Wrote 7 unit tests covering default language, namespace loading, language switching, fallback, and detection config
- All 1276 tests pass, typecheck clean, lint clean, build succeeds
- Learnings: i18next with `supportedLngs` auto-resolves unsupported language requests to fallbackLng (language prop becomes 'ro' not 'fr'); used bundled resources approach (not http-backend) since translation files are small and Vite tree-shakes unused code
---

## Iteration 8 - TASK-008: Create language switcher component
- Created `language-switcher.tsx` — a toggle button showing current language with flag emoji (🇷🇴 RO / 🇬🇧 EN)
- Calls `i18n.changeLanguage()` on click, toggles between ro and en
- Language persists via i18next's localStorage detector (key: `i18n_lang`)
- Integrated into `navbar.tsx` (desktop right section) and `navbar-mobile.tsx` (mobile menu footer)
- Wrote 6 component tests: render, accessible label, switch to EN, toggle back to RO, persistence across re-renders, aria-label update
- All 1282 tests pass, typecheck clean, lint clean
- Learnings: jsdom in vitest doesn't expose `localStorage.getItem` as a standard function — test persistence via i18n state rather than direct localStorage access
---

## Iteration 9 - TASK-009: Translate common namespace
- Both `ro/common.json` and `en/common.json` already had all required keys from TASK-007 setup
- Updated `PageError` component (`error-state.tsx`) to use `useTranslation('common')` for default title (`errors.somethingWentWrong`), default message (`errors.generic`), and retry button (`buttons.retry`)
- Changed EN `buttons.retry` from "Retry" to "Try again" to match existing codebase convention (30+ tests reference this label)
- `empty-state.tsx` and `loading-skeleton.tsx` receive all text via props — callers will pass translated strings in domain-specific tasks (TASK-010 to TASK-014)
- All 1282 tests pass, typecheck clean, lint clean
- Learnings: When translating a widely-used component, check if the translation value matches existing hardcoded text in tests across the codebase; changing the translation to match is simpler than updating dozens of tests
---

## Iteration 10 - TASK-010: Translate nav namespace
- Renamed `NavLink.label` to `NavLink.labelKey` with i18n translation keys (e.g. `links.home`)
- Updated `navbar.tsx`, `navbar-mobile.tsx`, `app-layout.tsx` to use `useTranslation('nav')` for all user-facing strings
- Added `menu.login`, `menu.register`, `menu.signOut`, `a11y.mobileNavigation` keys to both ro/en nav.json
- Updated navbar tests to wrap with `I18nextProvider` and set language to `'en'` in `beforeEach`
- Added i18n-specific test: verifies Romanian labels render when language is `'ro'`
- All 1283 tests pass, typecheck clean, lint clean (only pre-existing warning)
- Learnings: When converting hardcoded strings to i18n, set language to 'en' in test `beforeEach` so existing English text assertions don't break; aria-label changes need careful test updates
---

## Iteration 11 - TASK-011: Translate auth namespace
- Updated both `en/auth.json` and `ro/auth.json` with complete keys: login, register, forgotPassword, resetPassword, changePassword sections with page titles, form labels, placeholders, submit/loading buttons, links, error messages, success messages
- Added `passwordStrength` section (label, strengthLabel, ariaLabel, weak/fair/strong) and `validation` section (17 keys covering all Zod schema messages)
- Created i18n-aware schema factory functions: `createLoginSchema(t)`, `createRegisterSchema(t)`, `createForgotPasswordSchema(t)`, `createResetPasswordSchema(t)`, `createChangePasswordSchema(t)` — kept static schemas as fallback for tests
- Updated all 6 auth pages and 2 sub-components (`register-fields.tsx`, `reset-password-error.tsx`) to use `useTranslation('auth')` with `useMemo` for schema creation
- Updated all 5 auth test files: added `I18nextProvider` wrapper and `i18n.changeLanguage('en')` in `beforeEach`
- All 1283 tests pass, typecheck clean, lint clean (only pre-existing warning)
- Learnings: Zod + i18n requires factory pattern (`createSchema(t)`) with `useMemo` keyed on `t`; keep static schemas for unit tests that validate schema logic without rendering; all test render functions need `I18nextProvider` wrapping
---

## Iteration 12 - TASK-012: Translate search and booking namespaces
- Updated EN and RO search.json with 50+ new keys covering: page title, heading, form labels/validation/placeholders, results (with pluralization), empty/loading/error states, trip card (seats with pluralization, stops toggle, aria labels), trip detail page (back link, departure/arrival, from, stops count, cancelled badge, error/loading states, booking form with stop selection, seat count pluralization, confirm button)
- Updated EN and RO booking.json with 40+ new keys covering: seat states (available/selected/occupied/blocked/disabled), seat map (aria labels, no seats, premium), booking card (seats/order labels, details expand/collapse, loading), cancel dialog (title, description with orderId interpolation, keep/confirm/cancelling), my trips (heading, tabs, empty states per tab, load more, search CTA), status labels (both lowercase and UPPERCASE keys for direct mapping)
- Applied `useTranslation('search')` to: search.tsx, search-form.tsx, trip-card.tsx, trip/[id].tsx (including all sub-components: ResultsList, SearchContent, CitySelect, DateField, BookingForm, TripDetailContent, ErrorState, StopList, TripDetailSkeleton)
- Applied `useTranslation('booking')` to: my-trips.tsx, booking-card.tsx, seat-map.tsx (including sub-components: BookingList, MyTripsContent, BookingDetail, CancelDialog, SeatCell, SeatLegend)
- Used factory pattern for search form Zod schema (`createSearchFormSchema(t)` with `useMemo`)
- Updated my-trips.test.tsx: added `i18n.changeLanguage('en')` in beforeEach, changed status assertions from 'CONFIRMED'/'COMPLETED' to 'Confirmed'/'Completed'
- Removed unused `SEAT_STATE_KEYS` constant from seat-map.tsx (caught by lint)
- All 1283 tests pass, typecheck clean, lint clean (only pre-existing warning)
- Learnings: When translating status badges that display raw API enum values (e.g. 'CONFIRMED'), add both lowercase and UPPERCASE keys in translation files to support `t('status.CONFIRMED')` pattern; this avoids needing a mapping function
---

## Iteration 13 - TASK-013: Translate provider namespace
- All provider pages (dashboard, routes, fleet, schedules, drivers, tracking, profile) and dialogs (create-route, create-bus, edit-bus, create-schedule, create-driver, schedule-filter-bar, analytics-section) were already using `useTranslation('provider')` from previous iterations
- Both EN and RO provider.json files already had comprehensive translations with all required sub-sections (dashboard, routes, fleet, schedules, drivers, analytics, tracking, profile)
- Removed unused `SEAT_TYPE_LABELS` constant from seat-map-editor.tsx (replaced by i18n `getSeatTypeLabel` function)
- Fixed 3 test files (create-driver-dialog, edit-bus-dialog, create-schedule-dialog) and 1 test file (schedule-filter-bar) that needed `I18nextProvider` wrapping and `i18n.changeLanguage('en')` in `beforeEach`
- All 1283 tests pass, typecheck clean, lint clean
- Learnings: When components are translated incrementally across tasks, the test files may not get updated with i18n wrapping until a later task runs the full test suite and catches the failures; always run full test suite before marking a translation task complete
---

## Iteration 14 - TASK-014: Translate driver, admin, and tracking namespaces
- Admin pages were already fully translated with `useTranslation('admin')` from previous iterations
- Tracking page was already using `useTranslation('provider')` for the provider namespace tracking section; tracking components (sidebar, report dialog, active delays) already used `useTranslation('tracking')`
- Driver pages (trips.tsx, trip-detail.tsx, delay.tsx) were not translated — added `useTranslation('driver')` to all three pages and all sub-components (TripCard, TripListContent, DateNav, TripDetailSkeleton, TripDetailError, StopProgressTracker, LocationSharingToggle, TripInfoHeader, PresetButtons)
- Updated EN and RO driver.json with 60+ new keys covering: trips (status labels, navigation, error/empty states), tripDetail (loading, error, back, stop progress, location sharing, toast messages), delay (form labels, placeholders, character count, reasons, missing params)
- Replaced hardcoded STATUS_LABELS with i18n STATUS_KEYS map, removed hardcoded REASON_LABELS in favor of `t('delay.reasons.REASON')` pattern
- Fixed pre-existing admin/users.test.tsx failure: `getByText('Passenger')` found multiple matches (filter dropdown + user badge) — changed to `getAllByText().length >= 1`
- All 1283 tests pass, typecheck clean, lint clean
- Learnings: Role/status labels that appear both in filter dropdowns and in data badges cause `getByText` to fail with multiple matches; use `getAllByText` when the same label text is expected in multiple UI contexts
---

## Iteration 16 - TASK-016: Create frontend .env.example
- Created `apps/web/.env.example` with `VITE_API_URL` and descriptive comments explaining dev vs production usage
- Verified root `.gitignore` already excludes `.env` and `.env*.local`
- Note: actual `.env` uses `http://localhost:3003` (not `:3000/api/v1` as PRD suggested) — `.env.example` matches actual usage
- Learnings: Only one VITE_ env var is used in the entire frontend codebase
---

## Iteration 15 - TASK-015: Translate error boundary and 404 page
- Updated `error-fallback.tsx` to use `useTranslation('common')` for heading ("Error"/"Eroare"), default message, and retry button text
- Updated `not-found.tsx` to use `useTranslation('common')` for heading, description, and "Go home" link
- Added new translation keys to both EN and RO common.json: `errors.somethingWentWrongDefault`, `errors.errorHeading`, updated `errors.pageNotFoundDescription` to match component text
- Added `i18n.changeLanguage('en')` in `beforeEach` for all 3 test files (error-boundary, error-fallback, not-found)
- Added Romanian translation tests for both error-fallback and not-found
- All 19 error component tests pass, typecheck clean, lint clean
- Learnings: ErrorBoundary is a class component and can't use hooks directly — it delegates to ErrorFallback which handles the i18n; translation keys already existed in common.json from TASK-007 but needed additional keys for the default message and heading
---

## Iteration 17 - TASK-017: Add bundle size measurement
- Added `rollup-plugin-visualizer` as devDependency
- Configured vite.config.ts to conditionally include visualizer when `ANALYZE=true` env var is set
- Added `build:analyze` script to package.json that runs `ANALYZE=true vite build`
- `dist/stats.html` already covered by root `.gitignore` (`dist/` entry)
- Bundle baseline: Total JS 1.3MB (gzip ~300KB). Largest: index 411KB (127KB gz), live-map 156KB (46KB gz), types 56KB (13KB gz)
- Typecheck passes
- Learnings: `filter(Boolean)` on plugins array handles conditional plugin inclusion cleanly in Vite config
---

## Iteration 18 - TASK-018: Optimize hero image
- hero.png was 44KB (under 50KB threshold) but was not imported or referenced anywhere in the codebase
- Deleted the unused file per acceptance criteria
- Typecheck passes, no code changes needed
- Learnings: Always grep for actual imports/references before optimizing assets — unused assets should just be deleted
---
