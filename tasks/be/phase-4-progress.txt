## Iteration 1 - TASK-001: Configure CORS properly
- Configured @fastify/cors in app.ts with parseCorsOrigins() that reads CORS_ORIGIN env var (comma-separated)
- Dev mode auto-includes localhost:3000, 3001, 5173; production restricts to configured origins only
- Methods: GET, POST, PUT, PATCH, DELETE, OPTIONS; credentials: true; maxAge: 86400
- Exposed X-Request-Id header, allowed Content-Type + Authorization headers
- 7 integration tests covering: allowed origin, disallowed origin, preflight OPTIONS, exposed headers, parseCorsOrigins in dev/prod
- All 350 unit tests + 7 new integration tests pass, typecheck clean, lint clean
---

## Iteration 2 - TASK-002: Configure Helmet security headers
- Registered @fastify/helmet in app.ts with: strict CSP (default-src 'self', script-src 'self', frame-ancestors 'none'), HSTS (1 year, includeSubDomains, preload), X-Frame-Options DENY, referrer-policy strict-origin-when-cross-origin
- Helmet disabled in NODE_ENV=test to avoid noise in other integration tests
- Created dedicated integration test (helmet.integration.test.ts) that builds app with NODE_ENV=development to verify all 5 headers
- All 350 unit + 263 integration tests pass, typecheck + lint clean
---

## Iteration 3 - TASK-003: Configure rate limiting
- Registered @fastify/rate-limit globally (100 req/min per user, falls back to IP)
- Per-route overrides via config.rateLimit: auth routes (10/min per IP), search routes (30/min per IP)
- Added 429 handling in error-handler.ts since rate-limit plugin throws errors that reach the error handler
- RFC 9457 response with RATE_LIMITED code and Retry-After header
- Learnings: @fastify/rate-limit v10 throws errors that hit the error handler — must handle statusCode 429 explicitly. Don't use custom onRoute hooks; use config.rateLimit per-route instead. Use app.inject() with remoteAddress for consistent IP in tests.
- 4 integration tests: global headers, auth limit=10, search limit=30, 429 trigger
- All 350 unit tests pass, typecheck + lint clean
---

## Iteration 4 - TASK-004: Input sanitization and validation hardening
- All Zod schemas already had maxLength/min/max constraints from prior phases — verified in audit
- Added `.trim()` to all user-provided string fields in request schemas (email, name, phone, providerName, licensePlate, model, label, stopName, origin, destination, boardingStop, alightingStop, seatLabels items, note, language, avatarUrl)
- Added `bodyLimit: 1_048_576` (1MB) to Fastify config
- Added `genReqId: () => randomUUID()` and `requestIdHeader: 'x-request-id'` for request tracing
- Added onRequest hook to propagate x-request-id to responses
- Added 413 handling in error-handler.ts for payload too large errors (RFC 9457)
- 6 integration tests: UUID generation, client ID echo, unique IDs, 413 for oversized payload, trim passes validation, whitespace-only name rejected
- Learnings: Fastify bodyLimit errors hit the error handler as statusCode 413 — need explicit handling. `.trim()` before `.min(1)` properly rejects whitespace-only strings.
---

## Iteration 5 - TASK-005: Create audit logging service
- Created AuditActions constants (12 actions: auth, account, booking events) in domain/audit/audit-actions.ts
- Created AuditLogEntity and AuditLogInput interfaces in domain/audit/audit.entity.ts
- Implemented AuditService with fire-and-forget log() and paginated getByUser()
- 11 unit tests covering: all field mapping, null defaults, fire-and-forget error handling, all security event types, booking events, IP/userAgent capture, pagination skip/take, empty results, ordering, totalPages calculation
- Learnings: Prisma JSON field type requires intersection with `& object` for Record<string, unknown> to satisfy InputJsonValue — `(input.metadata as Record<string, unknown> & object)`. AuditLog model already existed in schema with composite index on [userId, createdAt].
---

## Iteration 6 - TASK-006: Add audit logging hooks to existing services
- Created `src/api/plugins/audit.ts` Fastify plugin that decorates request with `request.audit()` helper
- The helper extracts IP from x-forwarded-for (first entry) or socket, and user agent from headers automatically
- Integrated audit calls into auth routes: REGISTER, LOGIN_SUCCESS, LOGIN_FAILURE, LOGIN_LOCKED, LOGOUT, PASSWORD_RESET_REQUEST, PASSWORD_RESET_COMPLETE, PASSWORD_CHANGE
- Login failure/locked events use try/catch around authService.login() to audit on error before re-throwing
- Integrated audit calls into booking routes: BOOKING_CREATED, BOOKING_CANCELLED
- Added try/catch wrapper in AuditService.log() to handle synchronous errors (e.g., when prisma.auditLog is undefined in test mocks)
- 13 integration tests covering: helper availability, IP extraction from x-forwarded-for, user agent capture, all 8 auth events, both booking events
- Learnings: `decorateRequest('audit', null)` needs `as unknown as FastifyRequest['audit']` type cast. AuditService.log() must wrap in try/catch (not just .catch()) because if prisma.auditLog is undefined, the synchronous property access throws before any promise is created. Booking create uses interactive $transaction (callback), not array-style.
---
