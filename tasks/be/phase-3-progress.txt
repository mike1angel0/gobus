## Iteration 1 - TASK-001: Create search service
- Implemented SearchService with searchTrips and getTripDetails methods
- Created domain entities (SearchResult, SeatAvailability, TripDetail, TripStopTime)
- Created Zod schemas matching OpenAPI spec (searchQuerySchema, searchResultSchema, seatAvailabilitySchema, tripDetailSchema)
- Exported pure functions computeSegmentPrice and computeAvailability for testability
- 16 unit tests covering: segment pricing, availability computation, stop ordering, case-insensitive search, pagination, seat booking status
- Learnings: Prisma ScheduleStatus enum type must be cast with `as ScheduleStatus` in test mocks; search service filters in-memory after Prisma query since we need to match both origin and destination stops in correct order
---
## Iteration 2 - TASK-002: Create search API routes
- Implemented GET /api/v1/search and GET /api/v1/trips/:scheduleId as public endpoints (no auth)
- Serializers convert Date fields to ISO strings, tripDate uses .slice(0,10) for date-only format
- 15 integration tests covering: search with results, empty results, reversed stops filter, missing params, invalid date, pagination, trip detail with bookings, 404, no auth required, all seats unbooked
- Learnings: Integration tests use vitest.integration.config.ts (separate from unit); mock Prisma + env + logger pattern consistent across all route tests; public routes have no preHandler and no dependencies on auth plugin
---
## Iteration 3 - TASK-003: Create booking service
- Implemented BookingService with create, listByUser, getById, cancel methods
- Exported pure validation functions (validateSeatsOnBus, validateStopOrder) for testability
- create uses prisma.$transaction with Serializable isolation to prevent double-booking
- toBookingWithDetails maps Prisma records to BookingWithDetails shape (extracts seatLabels from bookingSeats)
- 26 unit tests covering: seat validation (non-existent, disabled, blocked), stop order validation, create with price calc, seat conflict 409, schedule not found, list with pagination and status filter, getById ownership enforcement, cancel states
- Learnings: Mock $transaction by extracting txClient and having $transaction call the callback with it; reuse computeSegmentPrice from search.service for segment pricing; ownership returns 404 not 403 per spec
---
## Iteration 4 - TASK-004: Create booking API routes
- Implemented 4 authenticated endpoints: GET /api/v1/bookings (list), POST /api/v1/bookings (create), GET /api/v1/bookings/:id (detail), DELETE /api/v1/bookings/:id (cancel)
- Created Zod schemas (createBookingBodySchema, listBookingsQuerySchema, bookingSchema, bookingWithDetailsSchema) with .describe() on all fields
- Separate serializers for list (bookingSchema, no schedule) vs detail (bookingWithDetailsSchema, with schedule/route/bus)
- 19 integration tests covering: list with pagination, empty list, status filter, create 201, seat conflict 409, schedule not found, invalid body, invalid stop order, get detail, ownership 404, cancel, cancel already-cancelled, auth 401
- Learnings: Auth plugin calls prisma.user.findUnique to check user status — integration tests MUST include user mock in mockPrisma and set mockUserFindUnique.mockResolvedValue in beforeEach; PRD said cancel returns 204 but OpenAPI spec says 200 with BookingWithDetailsDataResponse — always follow spec (source of truth)
---
## Iteration 5 - TASK-005: Create tracking service
- Implemented TrackingService with updatePosition, getByBusId, getActiveByProvider methods
- Created BusTrackingData domain entity in domain/tracking/tracking.entity.ts
- updatePosition validates bus existence (404), driver assignment via schedule.findFirst (403), then upserts BusTracking record
- scheduleId inferred from driver's assigned schedule if not provided in input
- toTrackingData mapper extracts clean entity from Prisma record
- 11 unit tests covering: upsert success, scheduleId inference, explicit scheduleId, bus not found 404, driver not assigned 403, null tripDate, assignment query filters, getByBusId found/null, getActiveByProvider with results/empty
- Learnings: BusTracking has @unique on busId — use prisma.busTracking.upsert for create-or-update; tracking 403 is correct per spec (not 404 like ownership checks) because it's about role-based assignment, not resource enumeration
---
## Iteration 6 - TASK-006: Create tracking API routes
- Implemented GET /api/v1/tracking/:busId (authenticated, returns 200 or 404) and POST /api/v1/tracking (DRIVER role only, validates assignment via service)
- Created Zod schemas (busIdParamSchema, trackingUpdateSchema, busTrackingSchema) with .describe() matching OpenAPI spec field-by-field
- serializeTrackingData converts Date fields (tripDate, updatedAt) to ISO strings; tripDate serializes to null when not set
- 13 integration tests covering: GET success, GET 404 no data, GET null fields, GET 401, POST success, POST with optional fields, POST 403 non-driver, POST 403 not assigned, POST 404 bus not found, POST 400 missing fields, POST 400 out of range, POST 400 unknown fields, POST 401
- Learnings: Integration tests use vitest.integration.config.ts; role check in route handler (not service) since service validates assignment but not role; strict() on request schema rejects unknown fields per coding standards
---
## Iteration 7 - TASK-007: Create delay service
- Implemented DelayService with create, getBySchedule, update methods
- Created DelayData domain entity in domain/delays/delay.entity.ts
- create validates driver assignment (schedule.driverId === user.id) or provider ownership (route.providerId === user.providerId)
- create uses $transaction to atomically deactivate previous active delays and create new one
- update checks ownership via delay -> schedule -> route -> providerId chain, returns 404 on mismatch
- 18 unit tests covering: getBySchedule with results/empty, create as driver/provider, deactivation, note handling, 404/403 errors, passenger rejection, transaction verification, update with ownership/partial fields/null note/not found
- Learnings: Mock $transaction pattern: vi.fn((cb) => cb(mockTx)) where mockTx has the Prisma methods used inside the transaction; for provider ownership check on delays, chain is delay -> schedule -> route -> providerId; role check (DRIVER/PROVIDER only) belongs in route handler, authorization check (assignment/ownership) belongs in service
---
## Iteration 8 - TASK-008: Create delay API routes
- Implemented 3 authenticated endpoints: GET /api/v1/delays (list by scheduleId+tripDate), POST /api/v1/delays (create, DRIVER/PROVIDER), PUT /api/v1/delays/:id (update, PROVIDER only)
- Created Zod schemas (listDelaysQuerySchema, createDelayBodySchema, updateDelayBodySchema, delaySchema) with .describe() matching OpenAPI spec
- serializeDelay converts Date fields (tripDate, createdAt) to ISO strings
- 24 integration tests covering: GET with results, GET empty, GET 400 missing params, GET 400 invalid date, GET 401, POST 201 driver, POST 201 provider with note, POST 403 passenger, POST 403 unassigned driver, POST 404 schedule not found, POST 400 missing/invalid/unknown fields, POST 401, PUT 200 update fields, PUT 200 deactivate, PUT 200 null note, PUT 403 non-provider, PUT 404 not found, PUT 404 other provider, PUT 400 unknown fields, PUT 401
- Learnings: DelayListResponse is not paginated (just { data: Delay[] }), unlike other list endpoints; for PUT update, provider ownership is enforced in service (returns 404 not 403); createAuthHeader supports providerId option for PROVIDER/DRIVER roles
---
## Iteration 9 - TASK-009: Create driver trip service
- Implemented DriverTripService with listTrips and getTripDetail methods
- Created DriverTrip and DriverTripDetail domain entities in domain/driver-trips/driver-trip.entity.ts
- listTrips filters by driverId + (tripDate OR daysOfWeek has dayOfWeek) using Prisma OR clause
- getTripDetail validates driver assignment (returns 404 not 403 to prevent enumeration), counts confirmed bookings, includes stop times and bus details
- Exported pure helper functions getDayOfWeek and resolveTripDate for testability
- 19 unit tests covering: getDayOfWeek for different days, resolveTripDate with/without date, listTrips with results/empty/multiple/default date, getTripDetail full detail/404/not assigned/null driver/booking count/bus details/empty stops/default date
- Learnings: PRD mentioned "passenger list, tracking status, active delay" in detail response but OpenAPI spec only has passengerCount/totalSeats — followed spec as source of truth per CLAUDE.md; documented in DOUBTS_AND_DECISIONS.md
---
## Iteration 10 - TASK-010: Create driver trip API routes
- Implemented GET /api/v1/driver/trips (list) and GET /api/v1/driver/trips/:scheduleId (detail) with DRIVER role enforcement
- Created Zod schemas (listDriverTripsQuerySchema, scheduleIdParamSchema, driverTripSchema, driverTripDetailSchema) with .describe() matching OpenAPI spec
- Serializers convert Date fields (departureTime, arrivalTime, tripDate, stop times) to ISO strings
- 15 integration tests covering: list with results, empty list, default date, multiple trips, 403 passenger, 403 provider, 400 invalid date, 401, detail with stops/passengers, default date detail, 404 not found, 404 not assigned, 403 non-driver, 401
- Learnings: Driver trip list response is non-paginated (just { data: DriverTrip[] }), similar to delays; reuse listDriverTripsQuerySchema for both list and detail date param
---
## Iteration 11 - TASK-011: Create admin service
- Implemented AdminService with listAllBuses (paginated, optional providerId filter) and toggleSeat (finds seat, updates isEnabled)
- Used module-level toBusEntity/toSeatEntity mapper functions (not class methods, since admin doesn't need bus-specific logic)
- 11 unit tests covering: paginated listing, empty list, provider filter, no filter, pagination skip/take, ordering, disable seat, enable seat, seat entity shape, seat not found 404, no update on not found
- Learnings: Admin service is simpler than other services — no ownership checks needed since ADMIN role is enforced at route level; reused BusEntity and SeatEntity from bus.entity.ts domain types
---
## Iteration 12 - TASK-012: Create admin API routes
- Implemented GET /api/v1/admin/buses (paginated, optional providerId filter) and PATCH /api/v1/admin/seats/:id (toggle isEnabled)
- Both endpoints require ADMIN role via requireAdmin preHandler
- Created Zod schemas (adminListBusesQuerySchema, toggleSeatBodySchema, busSchema, seatSchema) with .describe() matching OpenAPI spec
- serializeBus converts createdAt Date to ISO string; serializeSeat maps all primitive fields
- 14 integration tests covering: list with results, empty list, providerId filter, custom pagination, 403 passenger, 403 provider, 401, disable seat, enable seat, 404 not found, 400 missing body, 400 unknown fields, 403 non-admin patch, 401 patch
- Learnings: Admin routes are straightforward — role guard handles authorization, no ownership checks needed; reused idParamSchema from shared/schemas.ts for seat ID param
---
## Iteration 13 - TASK-013: Run Phase 3 quality gates
- All quality gates pass: typecheck, lint, format, 601 tests (99.41% coverage), build, spec:lint
- Zero `any` in production code (only in generated Prisma types)
- JSDoc coverage 100% on all exported functions/classes/methods
- Fixed formatting in 22 files with Prettier
- Learnings: format:check must be run and fixed before committing; generated Prisma files contain `any` but are excluded from the check
---
