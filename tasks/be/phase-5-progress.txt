## Iteration 1 - TASK-001: Fix unhandled promise rejection in server startup
- Added `.catch()` on `start()` call for unhandled buildApp failures
- Extracted `shutdown()` to module level with try/catch for error resilience
- Signal handlers use `void shutdown()` pattern for proper async handling
- Learnings: server.ts is excluded from coverage in vitest config; use `vi.resetModules()` + `vi.doMock()` for testing side-effect modules; intercept `process.on` to capture signal handlers without actually registering them
---

## Iteration 2 - TASK-002: Configure database connection pooling
- Added `max`, `min`, `idleTimeoutMillis` pool options to PrismaPg adapter in client.ts
- Added `DATABASE_POOL_MAX` env var (coerced number, default 10) to env schema
- Documented in .env.example
- Learnings: @prisma/adapter-pg v7.5 PrismaPg constructor accepts pg.PoolConfig as first arg (connectionString + pool options together); `onPoolError`/`onConnectionError` callbacks advertised in docs don't exist in this version's types — removed to keep typecheck clean; mock PrismaClient `$disconnect` with `vi.fn<() => Promise<void>>()` pattern for proper typing
---

## Iteration 3 - TASK-003: Fix search service in-memory pagination
- Replaced in-memory filtering + pagination with DB-level approach: raw SQL self-join on StopTime for origin/dest ordering check, separate count query without includes, LIMIT/OFFSET for pagination
- Only fetches full schedule data (with includes) for the paginated subset of IDs
- Learnings: Prisma `$queryRaw` with tagged template literals auto-parameterizes values (safe from SQL injection); BigInt returned by COUNT needs `Number()` conversion; mock `$queryRaw` as `vi.fn()` — Prisma calls it with a Sql object from the tagged template; integration tests use separate vitest config (`vitest.integration.config.ts`)
---

## Iteration 4 - TASK-004: Fix provider analytics sequential queries
- Wrapped all analytics queries in `$transaction` for consistent snapshot
- Parallelized 3 independent queries (booking aggregate, active schedules, revenue groupBy) with `Promise.all`
- Reused active schedule data for schedule→route mapping instead of fetching all schedules separately; only fetches unmapped schedules when revenue exists on non-active schedules
- Learnings: Mock Prisma `$transaction` with `vi.fn((fn) => fn(tx))` pattern — pass the mock tx object into the callback; expose `_tx` on the mock prisma for test access; use `mockResolvedValueOnce` chaining for groupBy that's called multiple times with different args
---

## Iteration 5 - TASK-005: Add missing database index
- Added `@@index([scheduleId])` to BusTracking model in schema.prisma
- Migration created and applied successfully
- Learnings: Simple schema-only tasks (adding indexes) don't require tests — just schema change, migration, and typecheck. Migration also picked up pending StopTime lat/lng columns that were in schema but not yet migrated.
---

## Iteration 6 - TASK-006: Fix tripDate format inconsistency across schemas
- Standardized `tripDate` to `format: date` (YYYY-MM-DD) everywhere — it's semantically a calendar date, not a timestamp
- Updated OpenAPI spec (8 response schemas + CreateScheduleRequest input), Zod schemas (7 response + 1 request), route serializers (5 files), and 7 integration test files
- Learnings: spec-conformance tests read from `spec/dist/openapi.json` (bundled spec) — must run `npx redocly bundle` after spec changes; pre-existing failures exist in driver-trips spec-conformance and cache-control integration tests (not related to this task); `z.string().date()` validates `YYYY-MM-DD` format strictly — datetime strings like `2025-06-01T00:00:00.000Z` are rejected
---

## Iteration 7 - TASK-007: Document 500 error responses on all endpoints
- Added `InternalServerError` response component to `spec/components/responses/errors.yaml`
- Added `'500': $ref` to all 50 operations across 12 path files using a Python script for consistency
- Bundled spec with `npx redocly bundle`
- Learnings: spec/dist/ is gitignored so don't try to commit bundled output; path files use inline schema refs for existing 4xx errors (not response component refs), but 500 uses `$ref` to named response component per acceptance criteria; total operations count is 50 (not 43 as stated in PRD)
---

## Iteration 8 - TASK-008: Document rate limiting in OpenAPI spec
- Added `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`, `Retry-After` headers to existing `TooManyRequests` response component in errors.yaml
- Added `'429': $ref` to all 50 operations across 12 path files
- Learnings: Python regex `\1` backreference inserts `\x01` byte when using `re.sub` with raw strings improperly — use raw strings for replacement or explicit group references; always verify YAML files with `xxd` or similar if spec:lint reports non-printable characters; the TooManyRequests response component already existed in errors.yaml but lacked headers
---
