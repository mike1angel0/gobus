# API base URL — the backend Fastify server
# In development, points to the local API server
# In production, nginx proxies /api requests so this can be a relative path
VITE_API_URL=http://localhost:3003
